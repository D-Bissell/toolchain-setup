---
- hosts: localhost
  tasks:
  - include_vars:
      file: ../defaults/main.yml # not required. The file name from which variables should be loaded.,If the path is relative, it will look for the file in vars/ subdirectory of a role or relative to playbook.
  - debug:
      var: media_root_path # not required. A variable name to debug.  Mutually exclusive with the 'msg' option.
      ignore_errors: yes
  - debug:
      msg: "volumes variable: {{ volumes }}"
      ignore_errors: yes
  - debug:
      var: "sab variable: {{volumes.sabnzbd }}"
      ignore_errors: yes
  - debug:
      var: "sab config variable: {{volumes.sabnzbd.config }}"
      ignore_errors: yes




# media_root_path:  /mnt/Media

# # Local folders to create for container volumes

# volumes:
# - sabnzbd:
#   config: /opt/sabnzbd/config
#   incomplete_downloads: "{{ media_root_path }}/incomplete"
#   completed_downloads: "{{ media_root_path }}/Downloads"
# - sonarr:
#   config: /opt/sonarr/config  
# - radarr:
#   config: /opt/radarr/config  